# Backend base URL (e.g., http://localhost:8000 or full domain)
REACT_APP_API_BASE_URL=http://localhost:8000/api

# WebSocket base URL (if backend supports it; keep for future use)
REACT_APP_WS_BASE_URL=ws://localhost:8000/ws

# Optional: App site URL for auth flows (if needed for redirects)
REACT_APP_SITE_URL=http://localhost:3000
